backend:
  name: git-gateway
  branch: main
local_backend: true
media_folder: 'src/data/uploads'

collections:
  - name: 'project'
    label: 'Project'
    folder: 'src/data/projects'
    create: true
    slug: '{{slug}}'
    fields:
      - label: 'Title'
        name: 'title'
        widget: 'string'
      - label: 'Publish Date'
        name: 'date'
        widget: 'datetime'
      - label: 'Featured Image'
        name: 'thumbnail'
        widget: 'image'
      - label: 'Body'
        name: 'body'
        widget: 'markdown'
